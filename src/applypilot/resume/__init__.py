"""Resume subpackage — decomposed from resume_json.py.

New code should import from specific submodules:
  - resume.validation: schema validation, security scanning
  - resume.extraction: profile data extraction helpers
"""
